[project]
name = "knowledge"
version = "0.1.0"
description = "AI developer assistant for the Kalisio ecosystem"

[tool.pytest.ini_options]
markers = [
  "General: General tests for the API",
]

[tool.coverage.run]
patch = ["subprocess"]
