name: knowledge
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - einops>=0.8.0
      - qdrant-client>=1.17.1
      - requests>=2.32.5
      - sentence-transformers>=5.3.0
